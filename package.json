{
  "name": "toyvdom",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "ISC",
  "scripts": {
    "clean": "rm -r dist && mkdir dist",
    "build": "yarn clean && tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^3.2.2"
  }
}
